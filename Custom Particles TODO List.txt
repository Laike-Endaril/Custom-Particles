NOTES =========================================================================================================================

A lot of the logic is moved to another thread, so it won't show up on profiling for the normal threads
Watch out for "Tick-based particle emitters skipped x ticks in the last second" in the console
"/omni threads" can be used for very rough profiling



TODO ==========================================================================================================================

Use a per-client-tick cache for blockstate results (one main cycle is one client tick, so it can be a local method cache in emitterLogicLoop() or one that is kept but cleared at the start of it)

Control range of block-finding checks with cull distance settings (currently in factory, but might want to move to emitter for this)
Add density control, make sure to scale based on total blocks in block-finding range



Add weather checks
Rain strength and thunder strength filters (<min> [max] for each)

Spawners...
* Entity (projectiles, bleeding/dripping)
* Entity collision (arrow hitting ground/water)
* Entity hit
* Block hit
* Item (eg. weapon trails, or a flaming sword, hopefully still flaming when on a display rack, etc)

Add better block color matching feature for block spawners? (wondering if eg. some modded leaf blocks use a pre-colored texture instead of block coloring?)

Look into a full rewrite for rendering *AND PHYSICS* on GPU side via SSBO and compute shader (and other shaders)
Take a look at this as a possible reference:
https://github.com/Ender-Development/EnderModpackTweaks/blob/master/src/main/java/io/enderdev/endermodpacktweaks/features/healthbar/HealthBarInstancingHelper.java

Add command to reload settings during runtime, or do it automatically via file name / size detection

Add line number, line position printout for parsing errors



Make block emitters use the sides of the block's bounding boxes...?
...and check for space internally...?
...and only check for adjacent block space if they need to (if the face is touching an adjacent blockspace)?

Make Winds of Time mod (wind mechanics API) and hook into it
Add wind multipliers for particles (random between min and max)
